import { ClearCaractersPipe } from './clear-caracters.pipe';

describe('ClearCaractersPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearCaractersPipe();
    expect(pipe).toBeTruthy();
  });
});
